---
sidebar_position: 3
---

# 工具系统

AIME Chat 提供强大的工具系统，让 AI Agent 能够执行各种实际操作。

## 内置工具

### 文件系统工具

| 工具 | 描述 | 用途 |
|------|------|------|
| **Read** | 读取文件内容 | 查看代码、配置文件等 |
| **Write** | 写入文件内容 | 创建或覆盖文件 |
| **Edit** | 编辑文件 | 精确修改文件内容 |
| **Grep** | 搜索文件内容 | 在代码库中搜索 |
| **Glob** | 文件名匹配 | 查找特定文件 |
| **Bash** | 执行命令 | 运行 shell 命令 |

#### 使用示例

```
用户：帮我查看 package.json 的内容

AI：[使用 Read 工具读取 package.json]
```

### 代码执行工具

| 工具 | 描述 | 用途 |
|------|------|------|
| **Python** | 执行 Python 代码 | 数据处理、计算、脚本 |
| **Node.js** | 执行 JavaScript 代码 | 脚本、API 调用 |

#### 使用示例

```
用户：帮我计算 1 到 100 的和

AI：[使用 Python 工具]
result = sum(range(1, 101))
print(result)  # 输出: 5050
```

### 网络工具

| 工具 | 描述 | 用途 |
|------|------|------|
| **Web Fetch** | 获取网页内容 | 抓取网页、API 调用 |
| **Web Search** | 搜索互联网 | 获取最新信息 |

### 图像工具

| 工具 | 描述 | 用途 |
|------|------|------|
| **Vision** | 图像分析 | 识别图像内容 |
| **RMBG** | 背景移除 | 去除图片背景 |

### 其他工具

| 工具 | 描述 | 用途 |
|------|------|------|
| **LibSQL** | 数据库操作 | 查询和管理数据 |
| **Todo** | 任务管理 | 创建和跟踪任务 |
| **Task** | 复杂任务 | 分解和执行复杂任务 |

## 启用工具

### 全局启用

1. 进入 **设置** → **工具**
2. 查看所有可用工具
3. 启用或禁用需要的工具

### 会话级别

在聊天输入框旁边：

1. 点击工具图标
2. 选择本次会话要使用的工具
3. 工具选择只影响当前会话

## MCP 协议支持

AIME Chat 支持 **Model Context Protocol (MCP)**，可以扩展第三方工具。

### 什么是 MCP？

MCP 是一个开放协议，允许 AI 应用连接各种数据源和工具。通过 MCP，您可以：

- 连接自定义工具服务器
- 使用社区开发的工具
- 构建自己的工具扩展

### 配置 MCP 服务器

1. 进入 **设置** → **MCP**
2. 点击 **添加服务器**
3. 填写服务器配置：

```json
{
  "name": "my-mcp-server",
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-filesystem"]
}
```

4. 保存并启用

### 常用 MCP 服务器

| 服务器 | 功能 |
|--------|------|
| `@modelcontextprotocol/server-filesystem` | 文件系统访问 |
| `@modelcontextprotocol/server-github` | GitHub 操作 |
| `@modelcontextprotocol/server-slack` | Slack 集成 |
| `@modelcontextprotocol/server-postgres` | PostgreSQL 数据库 |

## 工具安全

### 权限控制

- 文件操作默认限制在工作目录内
- 危险命令需要用户确认
- 可以配置工具的访问范围

### 最佳实践

1. 只启用需要的工具
2. 定期检查工具使用记录
3. 对敏感操作保持警惕

## 自定义工具

高级用户可以开发自定义工具：

1. 实现工具接口
2. 注册到工具系统
3. 配置工具参数

详细开发指南请参考 [开发文档](../development/custom-tools)。

## 常见问题

### 工具执行失败

1. 检查工具是否已启用
2. 确认相关依赖已安装
3. 查看错误日志获取详情

### MCP 服务器无法连接

1. 确认服务器配置正确
2. 检查网络连接
3. 确认服务器进程正在运行
















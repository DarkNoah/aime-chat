# 2026-08-20

## KnowledgeBaseSearch 工具清理 & 扩展列排查

- 从 `KnowledgeBaseSearch` 工具中删除了 `filter` 参数（legacy alias），只保留 `where`。涉及 `src/main/tools/knowledge-base/index.ts`（schema + execute）和 `index.test.ts`（删除 filter:null 引用及「where+filter 同时传」测试）。测试通过。
- 排查结论：`updateKnowledgeBaseItem` 在 content 变化重建 chunk 时，`insertChunkRows(kb, item.id, chunks, embeddings)` 未传第 5 个参数 `extendColumns`（默认 `[]`），导致重建后的 text chunk 扩展列全为 NULL，而 item 的 `extendData` 保留旧值 → 两处数据脱节，`where` 筛选会静默排除这些 chunk。
- 【已修复】在 `updateKnowledgeBaseItem` 重建 chunk 前，从 `item.extendData` + `kb.vectorStoreConfig.extendColumns` 构造 `extendColumns`（缺列用 null 兜底）传给 `insertChunkRows`。其余 3 处 insertChunkRows 调用本已正确传 extendColumns。tsc 无新增类型错误。
- 数据模型要点：扩展列（extendColumns）是 chunk 向量表 `kb_{id}_{vectorLength}` 的物理列，导入时每条 chunk 都复制一份 Item 的扩展列值；Item 另有 `extendData`（给 KnowledgeBaseGetItem 用）和 `metadata`（JSON）。`where` 只作用于 chunk 表的扩展列，不碰 Item 的 extendData/metadata。

---
sidebar_position: 2
---

# 基本使用

本指南将帮助您快速上手 AIME Chat 的基本功能。

## 初始化导向

![初始化导向](./images/aime_chat_init.gif)

## 界面概览

AIME Chat 的界面主要分为以下几个部分：

1. **侧边栏** - 会话管理、Agent 切换
2. **聊天区域** - 对话内容展示
3. **输入区域** - 消息输入和工具选择
4. **设置面板** - 模型和服务商配置

## 开始对话

### 1. 选择模型

在开始对话前，您需要先配置 AI 服务商并选择模型：

1. 点击设置图标进入设置页面
2. 选择 **AI 服务商** 选项卡
3. 启用您想使用的服务商并填写 API Key
4. 返回聊天页面，在模型选择器中选择模型

### 2. 发送消息

在输入框中输入您的问题或指令，按 `Enter` 或点击发送按钮即可发送消息。

:::tip 快捷键
- `Enter` - 发送消息
- `Shift + Enter` - 换行
- `Ctrl/Cmd + N` - 新建会话
:::

### 3. 使用工具

AIME Chat 支持多种工具调用。点击输入框旁的工具按钮可以查看和启用可用工具：

- **代码执行** - 通过 CodeExecution 运行 Python 代码
- **文件操作** - 读取、写入、搜索文件
- **网络请求** - 抓取网页、搜索信息
- **图像处理** - 图像分析、背景移除

## 会话管理

### 创建新会话

点击侧边栏的 **新建会话** 按钮或使用快捷键 `Ctrl/Cmd + N`。

### 管理历史会话

- 点击会话可切换到该会话
- 右键会话可进行重命名、删除等操作
- 会话会自动保存，关闭应用后不会丢失

### 导出项目聊天

项目聊天支持按线程导出完整历史：

1. 进入一个项目并打开任意聊天线程
2. 点击页面右上角的 **更多操作**，选择 **导出消息**
3. 勾选要导出的项目聊天线程
4. 选择格式并指定保存位置

| 格式 | 适合场景 |
|------|----------|
| Markdown (`.md`) | 阅读、归档与分享 |
| 原始 JSON (`.json`) | 保留 Mastra 原始消息和展开后的工具消息体 |
| Excel (`.xlsx`) | 在线程表和消息表中筛选、统计或复核 |
| Unsloth JSONL (`.jsonl`) | 准备训练数据，并保留工具调用与工具输出 |

导出对话框默认选中项目中的全部线程，也可以只导出当前线程或自定义组合。

详细说明见 [项目与文件工作区](../features/project-workspace)。

## 选择 Agent

AIME Chat 内置多种 Agent，针对不同场景优化：

| Agent | 描述 | 适用场景 |
|-------|------|----------|
| 默认 Agent | 通用对话助手 | 日常对话、问答 |
| 代码 Agent | 专注代码开发 | 编程、调试、代码审查 |

切换 Agent：
1. 在聊天页面点击 Agent 选择器
2. 选择适合您需求的 Agent

## 项目文件工作区

项目聊天侧边的文件树可以直接打开工作区文件：

- 文本和代码文件使用带语法高亮的编辑器，支持 `Ctrl/Cmd + S` 保存
- Markdown 可在富文本编辑与源码编辑之间切换
- 图片、音频、视频和 PDF 可直接预览
- 超过编辑上限的大型文本文件会以只读方式打开，避免误保存截断内容
- 关闭有未保存修改的文件前，应用会提示确认

## 外观设置

进入 **设置 → 外观** 后可以配置：

- 主题模式：浅色、深色或跟随系统
- 主题色：使用内置预设或自定义颜色
- 侧边栏背景与聊天背景：两者可分别选择图片、调整透明度和模糊度

背景图片支持 JPG、PNG 和 WebP，单张最大 10 MB。导入后图片会复制到应用的用户资料目录；恢复默认外观会清除主题色与两张背景图，但保留当前主题模式。

## 浏览器实例

需要浏览器自动化或复用本地登录状态时，进入 **设置 → 实例管理**：

1. 选择系统检测到的浏览器用户数据目录，或指定自定义目录
2. 为内置配置选择已安装的 Google Chrome、Microsoft Edge 或 Chromium
3. 按需配置远程调试端口和无头模式
4. 启动实例；使用结束后可在同一页面停止

未安装的浏览器会显示为不可用。AIME Chat 只会为实际检测到的浏览器提供可执行文件路径。

跨平台检测路径和排障方式见 [浏览器实例](../features/browser-instances)。

## Bash 的 Python 运行器

在 Agent 的 Bash 工具配置中，可以选择：

- **独立运行器（推荐）**：使用 AIME Chat 管理的 Python 环境，依赖与系统环境相互隔离
- **系统运行器**：使用登录 Shell 中可用的系统 Python 与既有环境配置

如果独立运行器尚未安装，请先在 **设置 → 运行库** 中安装 UV / Python。

## 语言设置

支持中文和英文界面：

1. 进入设置页面
2. 选择 **通用** 选项卡
3. 在语言选项中选择您偏好的语言

## 下一步

- 了解如何 [配置 AI 服务商](./ai-providers)
- 探索 [知识库功能](../features/knowledge-base)
- 学习 [工具系统](../features/tools) 的高级用法














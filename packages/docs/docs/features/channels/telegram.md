---
sidebar_position: 1
---

# Telegram 连接

AIME Chat 支持将 AI 能力扩展到 Telegram 场景中使用。完成连接后，你可以在 Telegram 中直接与 AI 助手对话，或将其用于通知、问答、远程操作入口等场景。

## 连接步骤
- 设置 - 频道管理 - 添加Telegram
- 在Telegram App 中, 添加@BotFather, 对话 `/newbot`, 输入bot名称, 获得 BotToken 如`12345678:zzxzxczxc`
- 将获得的 BotToken 填入 aime-chat 界面中
- 生成配对码如 `123456`
- 根据提示在Telegram中你创建的bot对话中,输入 `/pair 123456`
- 测试对话是否成功

## 适用场景

Telegram 连接适合以下场景：

- 在手机或平板上随时与 AI 助手对话
- 通过消息方式接收任务结果、状态通知或摘要信息
- 将 Telegram 作为远程触发入口，发起简单的问答或工作流
- 在团队频道或个人聊天中快速访问常用 AI 能力

## 连接后可以做什么

接入 Telegram 后，常见能力包括：

- **消息对话**：像在桌面端一样向 AI 提问
- **远程触达**：不在电脑前时也能继续使用 AI
- **轻量工作流触发**：通过消息发起简单任务
- **通知回传**：接收执行结果、摘要或提醒信息

具体可用能力会受到你当前 Agent、模型配置、工具权限和频道实现方式的影响。

## 典型使用方式

一个常见的 Telegram 使用流程如下：

1. 在 AIME Chat 中启用 Telegram 频道能力
2. 配置 Telegram 所需的连接信息
3. 选择要绑定的 Agent、模型或默认行为
4. 在 Telegram 中向机器人发送消息
5. 由 AIME Chat 接收消息、处理请求并返回结果

```text
Telegram 用户消息
    ↓
Telegram Bot / Webhook
    ↓
AIME Chat
    ↓
模型 / Agent / 工具系统
    ↓
返回处理结果到 Telegram
```

## 常见问题

### Telegram 连接适合替代桌面端吗？

更适合作为桌面端的补充。桌面端适合复杂任务、多工具协作和长上下文操作；Telegram 更适合快速提问、移动访问和通知触达。

### Telegram 中的回复效果和桌面端一致吗？

核心模型能力通常一致，但频道环境下的交互方式、上下文长度、工具权限和展示形式可能不同，因此最终体验会有差异。

### 是否可以给 Telegram 绑定专用 Agent？

可以，这通常是更推荐的做法。为 Telegram 单独配置一个 Agent，可以更好地控制回复风格、工具权限和使用边界。

## 下一步

如果你已经准备好使用频道能力，建议继续：

- 明确 Telegram 的使用场景
- 选择合适的 Agent 与模型
- 配置最小必要权限
- 小范围测试消息收发、上下文连续性和错误处理体验

---
sidebar_position: 2
---

# 项目与文件工作区

项目把本地目录、聊天线程、Agent 上下文、文件编辑和后台任务组织在同一空间中。适合代码仓库、长期研究或需要持续维护文件的工作。

## 创建项目

1. 进入 **项目** 页面并选择新建项目
2. 输入项目名称
3. 选择一个本地目录
4. 按需选择 Code 或 Work 标签
5. 创建后进入项目聊天

### 从 GitHub 克隆

新建项目时可以额外填写 GitHub 仓库地址。AIME Chat 会先把仓库克隆到所选目录，再创建项目。

- 支持标准 HTTPS、SSH URL 和 `git@github.com:owner/repository.git` 格式
- 目标目录必须不存在或为空，并且父目录已经存在
- 本机必须已安装 Git，私有仓库需要预先配置可用的凭据或 SSH Key
- 克隆过程不会打开交互式凭据提示；认证失败时请先在终端验证仓库访问

## 文件浏览与搜索

项目聊天中的文件面板以项目目录为边界：

- 展开目录并点击文件即可在工作区打开
- 可在文件树中搜索文件名或内容
- 文件预览面板可以调整宽度，适合与聊天并排工作
- 聊天中的文件操作和 Agent 工具默认使用项目目录作为工作区

## 预览与编辑

| 文件类型 | 工作区行为 |
|----------|------------|
| 文本与代码 | 使用 CodeMirror 编辑，并根据扩展名启用语法高亮 |
| Markdown | 可在 Markdown 编辑器与源码模式之间切换 |
| 图片 | 应用内预览，并支持放大查看 |
| 音频 / 视频 | 使用系统媒体控件播放 |
| PDF | 在内嵌预览器中查看 |
| 3D 模型（GLB、glTF、FBX、OBJ、STL） | 交互式预览，支持旋转、缩放、平移、放大与线框切换 |
| 其他二进制文件 | 提示使用系统应用打开 |

代码高亮覆盖 JavaScript/TypeScript、JSON、HTML、CSS、Markdown、Python、SQL、YAML 和 XML 等常见格式。

### 查看 3D 模型

点击项目文件树中的 `.glb`、`.gltf`、`.fbx`、`.obj` 或 `.stl` 文件，即可打开 3D 预览。拖动和滚轮可调整视角；右上角按钮用于 **显示线框 / 隐藏线框** 和放大 / 缩小预览。材质视图使用本地生成的环境光照，无需额外下载远程 HDR。

3D 预览用于查看模型，不提供几何或材质编辑。切换线框不会改写原文件。通过 [Generate3D](./tools#3d-模型生成) 生成的 GLB 可在同一工作区继续查看；glTF 等引用外部资源的文件需要保留关联文件。

### 保存与大文件保护

- 使用 `Ctrl/Cmd + S` 保存当前文本文件
- 有未保存修改时会显示状态标记
- 关闭未保存文件前会要求确认
- 超过 2 MiB 的文本文件只显示截断后的只读预览，避免把不完整内容写回原文件

## 项目聊天线程

同一项目可以维护多个聊天线程。每个线程共享项目范围，但保留自己的消息历史；项目级任务视图还会汇总这些线程启动的后台 Bash 会话。

### 时间线与项目记忆

创建项目时，**开启时间线** 默认开启；可在创建、编辑弹窗或时间线面板中切换。对话完成后会自动整理本次实质任务的总结和产出，生成总结会额外消耗少量 Token。

每条时间线对应 **Project Memory（项目记忆）** 知识库中的一个独立 Markdown 条目，条目名称使用任务摘要标题，正文保留标题、详细总结和产出。项目、线程、运行标识及时间信息仅保存在元数据中。知识库配置同名扩展列，供检索时按项目过滤。时间线不再使用独立业务表，旧数据会在成功迁移后移除旧表；迁移失败时保留原数据，并在后续访问时重试。

`MemoryRead`、`MemorySearch`、`MemoryList` 以及写入、删除工具支持可选 `type: "global" | "project"`。省略时，项目聊天默认使用当前项目记忆，普通聊天使用全局记忆；在普通聊天中显式指定 `project` 会报错。同名时间线仍是独立条目，可用列表或搜索结果中的 `itemId` 调用 `MemoryRead` 精确读取。列表使用 `offset`（从 0 开始）和 `limit`（默认 20，最大 100）分页，不提供 `page` 参数。

```json
{ "type": "project", "offset": 0, "limit": 20 }
```

项目聊天会在全局记忆摘要之后，排除当前聊天线程的记录，再注入当前项目最新的 10 条时间线条目标题，按任务开始时间倒序排列，并提示通过 Memory 工具读取详情；没有其他记录时不注入消息，也不自动注入条目正文。项目记忆未配置向量模型时使用 BM25 全文检索。删除项目或聊天线程时，会清理相应来源的项目记忆。

### 导出聊天历史

打开任意项目聊天后，从右上角的更多菜单选择 **导出消息**：

1. 选择一个、多个或全部项目线程
2. 选择保存格式
3. 指定本地文件路径并导出

| 格式 | 内容 |
|------|------|
| Markdown | 适合阅读、归档和分享的完整对话 |
| 原始 JSON | Mastra 原始消息与展开后的模型消息 |
| Excel | 分为线程和消息工作表，便于筛选与统计 |
| Unsloth JSONL | 一行一个会话，保留工具调用、`role=tool` 与工具输出 |

Unsloth 导出会移除中断提示等训练噪声，但训练前仍应检查隐私、授权和数据质量。

## 使用建议

- 将一个稳定目录绑定到一个项目，避免在项目使用期间移动或重命名根目录
- 用多个聊天线程拆分功能开发、调研和运营任务，再通过项目范围查看后台进程
- 大型二进制资料可在工作区预览；需要结构化读取时，让 Agent 使用 Read、OCR 或代码执行工具
- 导出训练数据前先用 Markdown 或 Excel 格式抽查消息和工具结果
